# API 密钥累计费用溢出修复 Impl

## 任务概述

修复 API Key 生命周期累计费用达到 MySQL 字段上限后，本地计费事务整体回滚的问题。修复后，套餐额度耗尽的请求可以继续按混合计费或余额计费正常落账。

本次不改变套餐优先、余额兜底等业务规则，也不自动补扣历史异常请求。

## 文件变更清单

- `backend/app/models/user.py`
  - 将 `UserApiKey.total_cost` 从 `DECIMAL(12,6)` 扩大为 `DECIMAL(20,6)`。
- `backend/sql/init.sql`
  - 统一新建 `user_api_key` 表的累计费用字段精度。
- `backend/sql/initData.sql`
  - 统一后端初始化数据脚本的累计费用字段精度。
- `sql/initData.sql`
  - 将历史数据库快照中的 `DECIMAL(10,6)` 修正为 `DECIMAL(20,6)`。
- `backend/sql/upgrade_user_api_key_total_cost_20260814.sql`
  - 新增幂等生产升级脚本。
- `backend/test_user_api_key_total_cost_schema.py`
  - 新增 ORM、初始化 SQL 和升级 SQL 的结构一致性回归测试。
- `md/plan-API密钥-累计费用溢出修复-20260814.md`
  - 记录技术方案、风险边界和实施状态。

## 核心代码说明

### 字段扩容

`user_api_key.total_cost` 是 API Key 生命周期累计值，不是单次请求费用。生产历史字段 `DECIMAL(10,6)` 仅能累计到 `9999.999999`，因此扩大为 `DECIMAL(20,6)`，保留原有六位小数精度并显著提高整数范围。

### 幂等升级

升级脚本通过 `information_schema.columns` 检查：

- 表和字段存在；
- 字段不是 DECIMAL、精度小于 20，或小数位不是 6。

只有条件满足时才执行 `ALTER TABLE ... MODIFY COLUMN`。重复执行时不会再次修改容量至少为 20 位且 scale 为 6 的字段，也不会主动缩小已有的更高精度字段。脚本将会话锁等待限制为 15 秒，避免长时间阻塞线上数据库。

### 一致性保护

回归测试同时检查：

- SQLAlchemy ORM 精度；
- 三份初始化/数据库快照中 `user_api_key` 表的字段精度；
- 升级脚本包含结构检查和目标 `MODIFY COLUMN`。

## 测试验证

已运行：

```bash
python3 -m py_compile backend/app/models/user.py backend/test_user_api_key_total_cost_schema.py
```

结果：通过。

已运行：

```bash
PYTHONPATH=backend /Volumes/environment/Python/anaconda3/envs/ai-invoke-service/bin/python \
  -m unittest backend.test_user_api_key_total_cost_schema
```

结果：`Ran 3 tests ... OK`。

已运行：

```bash
PYTHONPATH=backend /Volumes/environment/Python/anaconda3/envs/ai-invoke-service/bin/python \
  -m unittest backend.test_agent_api_base_url
```

结果：`Ran 14 tests ... OK`。测试环境 Redis 不可用，相关缓存被自动禁用，不影响本次测试结果。

已运行：

```bash
git diff --check
```

结果：通过。

已在隔离的本地 MySQL 实例实际执行升级脚本：

1. 创建 `DECIMAL(10,6)` 模拟字段并写入生产临界值 `9999.999511`。
2. 第一次执行升级脚本后，字段为 `DECIMAL(20,6) NOT NULL DEFAULT 0.000000`，原值保持不变。
3. 第二次执行同一脚本，无报错且字段结构、原值保持不变。
4. 扩容后将累计费用增加 `0.600000`，成功得到 `10000.599511`。
5. 分别验证目标表不存在、目标字段不存在时，脚本均安全退出。

本地隔离实例版本为 MySQL 9.4；生产环境为 MySQL 8.0.45。脚本使用的 `information_schema`、存储过程、`DECIMAL(20,6)` 和 `lock_wait_timeout` 均属于 MySQL 8 支持能力，部署时会在生产 MySQL 8 上再次执行两次并核验结果。

## 部署与验证

已于 2026-08-14 完成生产部署：

- 修复提交 `ea6890e fix: widen API key cumulative cost` 已推送到 GitHub `main`，生产服务器已拉取该提交。
- 升级前已备份生产表结构至 `/root/user_api_key_schema_before_20260814.sql`。
- 在生产 MySQL 8.0.45 上连续执行两次 `backend/sql/upgrade_user_api_key_total_cost_20260814.sql`，两次均成功，验证脚本可重复执行。
- `user_api_key.total_cost` 已由 `DECIMAL(10,6)` 扩大为 `DECIMAL(20,6)`。
- 后端已在 8085 端口重启，`GET http://127.0.0.1:8085/health` 返回 `{"status":"ok"}`。
- 本次没有前端文件变更，因此未执行前端构建或 nginx 静态资源发布。

使用 `dangdangdang` 现有 `codex` API Key 发起一次最小真实请求验证，仅在服务器内存中读取密钥，未写入日志或文档：

- 请求 ID：`560cf1cf-ff11-421c-bbef-1a107597c37c`；模型：`gpt-5.6-sol`；状态：`success`。
- 计费模式：`balance`；本次记录费用：`0.001283 USD`。
- 用户余额：`345.380743` -> `345.379461`，确认余额扣减已落库。
- API Key 累计费用：`9999.999511` -> `10000.000794`，确认累计值已成功越过旧字段上限。
- 验证请求之后新增的 `total_cost` 越界计费异常数量为 `0`。

生产修复前已存在 3538 条相关记账失败记录。历史未记账费用估算高于用户当前余额，因此本次遵循风险边界，不对历史请求自动补扣；如需追缴，应由业务侧另行确定规则并单独实施。

## 待优化项

- 历史未记账请求的费用高于用户当前余额，需要业务侧确认欠费处理规则后另行处理。
- 可后续增加累计统计字段接近上限的监控，提前发现其他历史结构漂移。
