# 回退缓存优化保留重试容错 Impl

## 任务概述

按用户要求放弃 2026-06-11 的缓存策略、渠道粘性和 Anthropic 映射自动 Responses 路由优化，保留上游失败重试和错误脱敏容错修复。

指定请求 `9cb9c346-339a-4015-a684-33442242c2b1` 查询结果显示，该请求发生在服务器回滚到 `e4c3775` 后，错误日志为：

```text
Upstream returned HTTP 503: {"error":{"message":"auth_unavailable: no auth available (providers=codex, model=gpt-5.4)","type":"server_error","code":"internal_server_error"}}
```

该旧版本不包含 `088070d` 的错误脱敏修复，因此用户侧可能看到上游原始错误。

## 文件变更清单

- 反向撤销缓存相关提交：
  - `3bc1912` Anthropic 映射 Responses 路由优化。
  - `d436b64` 上游缓存命中策略和渠道粘性。
  - `f8f399d` prompt cache normalize scope。
  - `50164a5` Anthropic prompt cache 策略优化。
- 保留 `088070d`：
  - 上游失败重试。
  - 503/429/403 等上游异常的通用用户提示。
  - 上游原始错误仅保留到日志详情。
- `backend/test_proxy_retry_error_sanitization.py`
  - 增加 `auth_unavailable providers=codex model=gpt-5.4` 的回归测试。

## 验证

已执行：

```bash
env PYTHONPATH=backend python -m py_compile backend/app/services/proxy_service.py backend/app/services/anthropic_prompt_cache_service.py backend/test_proxy_retry_error_sanitization.py
env PYTHONPATH=backend python -m unittest backend/test_proxy_retry_error_sanitization.py
```

结果：28 个测试通过。

## 预期效果

- `claude-opus-4-8 -> gpt-5.5` 不再应用今天新增的自动路由判断，回到显式 `responses:` 才走 Responses 的配置行为。
- 不再启用今天新增的渠道粘性/缓存命中策略。
- 用户请求上游遇到 `auth_unavailable`、`providers=codex`、上游 503 等错误时，用户侧只看到 `调用失败，渠道异常，请稍后重试`，后台日志详情仍可看到上游原始错误。
