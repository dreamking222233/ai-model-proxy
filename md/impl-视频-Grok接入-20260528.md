## 任务概述

完成 Grok 视频生成模型接入，支持通过当前系统调用 grok2api 的异步视频任务接口。

新增对外接口：

- `POST /v1/videos`
- `POST /v1/created/video`
- `GET /v1/videos/{video_id}`
- `GET /v1/videos/{video_id}/content`

支持能力：

- 文生视频
- 图生视频，`input_reference[]` 最多 7 张，超过返回 400
- 异步任务创建与任务查询
- 视频内容代理下载
- 成功创建任务后按媒体积分扣费并写入请求日志

## 文件变更清单

- `backend/app/api/proxy/video_proxy.py`
- `backend/app/main.py`
- `backend/app/api/user/models.py`
- `backend/app/services/proxy_service.py`
- `backend/app/services/model_service.py`
- `backend/app/schemas/model.py`
- `backend/app/test/test_grok_video_proxy.py`
- `backend/sql/upgrade_grok_video_model_20260528.sql`
- `backend/sql/init.sql`
- `backend/sql/initData.sql`
- `sql/initData.sql`
- `frontend/src/views/admin/ModelManage.vue`
- `frontend/src/components/chat/ModelSelector.vue`
- `frontend/src/views/user/ModelList.vue`
- `frontend/src/views/user/QuickStart.vue`
- `docs/grok-video-integration.md`
- `md/plan-视频-Grok接入-20260528.md`

说明：工作区中已有 `backend/app/test/test_subscription_compatibility.py` 与计费预检相关未提交变更，本任务未回滚该变更。

## 核心代码说明

### 1. 视频路由

新增 `backend/app/api/proxy/video_proxy.py`：

- 创建接口解析 `multipart/form-data`
- 支持 `input_reference[]` 和兼容字段 `input_reference`
- 注册任务查询与视频内容下载接口

### 2. 视频上游转发

在 `ProxyService` 中新增视频处理链路：

- 校验 `model`、`prompt`、`seconds`、`size`、`resolution_name`、`preset`
- 要求统一模型 `model_type=video`
- 支持 `billing_type=image_credit/free`
- 通过 `model_channel_mapping` 查找 OpenAI 兼容渠道
- 将创建请求转发到上游 `/v1/videos`
- 将查询和下载请求转发到上游 `/v1/videos/{video_id}` 与 `/content`

### 3. 任务路由与权限

创建任务成功后：

- 将上游返回的 `video_id` 写入 `request_log.upstream_session_id`
- 同时保存短期内存路由缓存，降低查询时数据库读取

查询或下载时：

- 先校验当前用户是否拥有对应 `video_id` 的成功请求日志
- 再按日志中的渠道转发到上游
- 避免无归属探测所有上游导致多租户任务泄露
- 视频内容下载使用流式代理，避免大文件一次性读入内存

### 4. 计费与日志

视频生成复用图片积分账户作为媒体积分：

- `billing_type=image_credit`：按 `image_credit_multiplier` 扣减
- `billing_type=free`：不扣减积分，仅记录日志
- 请求日志记录 `request_type=video_generation`
- 视频尺寸记录在 `image_size` 字段，扣减积分记录仍写入 `image_credit_record`

### 5. 管理端与文档

- 管理端模型类型新增“视频”
- 图片积分文案调整为媒体积分，兼容图片与视频
- 用户模型列表补充视频类型筛选和视频模型能力字段
- 用户快速开始新增视频生成、查询、下载示例
- 新增 `docs/grok-video-integration.md`

## Review 后迭代

针对自审结果已修复：

- `/v1/videos/{video_id}/content` 改为 `StreamingResponse` 流式代理，并转发 `content-length`、`content-disposition`。
- `backend/sql/initData.sql` 与 `sql/initData.sql` 均补齐 `grok-imagine-video` 初始化数据；`backend/sql/initData.sql` 同步补齐媒体积分相关字段。
- `seconds`、`size`、`resolution_name`、`preset` 在进入渠道循环前完成预校验，避免客户端参数错误触发多渠道请求。
- 参考图超过 7 张时返回 `TOO_MANY_VIDEO_REFERENCES`，不再静默截断。

## 测试验证

已执行：

```bash
python -m py_compile backend/app/api/proxy/video_proxy.py backend/app/main.py backend/app/services/proxy_service.py backend/app/services/model_service.py backend/app/schemas/model.py backend/app/api/user/models.py
python -m unittest app.test.test_grok_video_proxy
python -m unittest app.test.test_openai_image_channel
python -m unittest app.test.test_grok_video_proxy app.test.test_openai_image_channel
python -m unittest app.test.test_subscription_compatibility
python -m unittest app.test.test_grok_video_proxy app.test.test_openai_image_channel app.test.test_subscription_compatibility
git diff --check
npm run build
```

结果：

- Python 语法检查通过
- 新增 Grok 视频代理测试通过
- OpenAI 图片渠道回归测试通过
- 订阅兼容测试通过
- `git diff --check` 通过
- 前端生产构建通过，仅保留既有资源体积 warning

## 待优化项

- 当前未新增持久化视频任务表，任务归属复用 `request_log.upstream_session_id`；如后续需要展示视频任务列表，可新增专用任务表。
- 当前暂未接入 `/v1/chat/completions` 的 `video_config` 视频模式；本次优先完成用户要求的 `/v1/created/video` 和 `/v1/videos` 独立接口。
