## 用户原始需求

当前 `sql` 目录下文件很多，只保留刚刚上传的 `sql/initData.sql` 即可。

## 技术方案设计

1. 仅处理仓库根目录 `sql/`。
2. 保留 `sql/initData.sql`。
3. 删除 `sql/` 下其余所有 SQL 文件。
4. 不修改 `backend/sql/`，避免影响后端目录中保留的升级与部署脚本。
5. 提交并推送到 `main`。

## 涉及文件清单

- `sql/` 目录下除 `initData.sql` 外的所有 `.sql` 文件
- `md/plan-sql目录精简-20260513.md`

## 实施步骤概要

1. 审查当前 `sql/` 目录。
2. 删除非 `initData.sql` 文件。
3. 提交并推送。
