# 回退缓存优化保留重试容错 Plan

## 用户原始需求

放弃当前缓存策略和路由优化，只保留最新版本中的上游失败重试和错误容错修复；用户侧不能再看到 `Upstream returned HTTP 503: ... providers=codex ...` 这类上游原始错误。参考请求：`9cb9c346-339a-4015-a684-33442242c2b1`。

## 技术方案设计

当前提交关系：

- `50164a5`：Anthropic prompt cache 策略优化。
- `f8f399d`：prompt cache normalize scope。
- `d436b64`：上游缓存命中策略和渠道粘性。
- `088070d`：上游失败重试机制和错误脱敏容错。
- `3bc1912`：Anthropic 映射 Responses 路由优化。

处理方案：

1. 基于最新 `main` 反向撤销缓存相关提交：`3bc1912`、`d436b64`、`f8f399d`、`50164a5`。
2. 保留 `088070d` 中的重试、上游状态重试、错误脱敏和日志保留逻辑。
3. 增加/保留测试，覆盖上游 503 `auth_unavailable providers=codex` 的用户可见错误脱敏。
4. 推送 GitHub 后在服务器拉取最新提交，重启 8085 后端。
5. 监控 `首尔-claude` 缓存与错误情况。

## 涉及文件

- `backend/app/services/proxy_service.py`
- `backend/test_proxy_retry_error_sanitization.py`
- 反向撤销缓存相关 SQL / 前端配置 / 文档 / 测试文件。
- `md/impl-回退缓存优化保留重试容错-20260611.md`
- `md/review-回退缓存优化保留重试容错-20260611.md`

## 实施步骤

1. 查询指定请求日志，确认原始上游错误泄漏来源。
2. 执行缓存相关提交反向撤销。
3. 解决冲突并保留 `088070d` 的容错逻辑。
4. 运行后端语法检查和错误脱敏测试。
5. 提交、推送、服务器部署和日志监控。
