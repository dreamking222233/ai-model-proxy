# 用户-模型展示 Plan

## 用户原始需求

当前版本更新后 `user/models` 页面模型没有价格和上下文信息，需要查看 git 记录恢复展示，然后提交 GitHub 并在服务器拉取更新。

## 技术方案设计

- 对比历史版本 `frontend/src/views/user/ModelList.vue`，确认价格和上下文信息曾由模型卡片中部价格区、底部标签区展示。
- 在当前页面恢复历史卡片结构，同时保留当前版本新增的按请求计费筛选能力。
- 对 `billing_type === request` 增加单次请求价格展示和“按请求计费”标签。
- 仅提交本次修复相关文件，避开工作区已有其他未提交改动。

## 涉及文件清单

- `frontend/src/views/user/ModelList.vue`
- `md/plan-用户-模型展示-20260608.md`
- `md/impl-用户-模型展示-20260608.md`

## 实施步骤概要

1. 查看 git 历史，定位旧版价格/上下文展示结构。
2. 恢复模型卡片模板和样式。
3. 兼容按请求计费展示。
4. 执行前端校验/构建。
5. 提交并推送 GitHub。
6. 服务器拉取代码并重新部署前端。
