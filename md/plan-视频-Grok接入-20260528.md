## 用户原始需求

深度阅读当前目录下项目及相关文档，结合 grok2api 视频生成接口文档，为当前系统新增 Grok 视频生成模型接口开发。

目标能力：

- 接入 `grok-imagine-video`
- 用户可通过 `/v1/created/video` 生成视频
- 支持文生视频与图生视频，图生视频最多 7 张参考图
- 支持异步生成、任务查询和视频内容获取
- 结合当前系统架构完成渠道路由、计费、日志与文档

## 技术方案设计

当前系统已有 OpenAI/Gemini 图片接口、统一模型、渠道映射、图片积分与请求日志能力。视频生成与图片生成同属媒体类请求，推荐复用现有 `image_credit` 余额体系作为媒体积分计费，不新增余额表，使用 `request_type=video_generation` 区分日志。

上游 grok2api 已确认支持：

- `POST /v1/videos`
- `GET /v1/videos/{video_id}`
- `GET /v1/videos/{video_id}/content`

本次新增当前系统对外接口：

- `POST /v1/videos`
- `POST /v1/created/video`
- `GET /v1/videos/{video_id}`
- `GET /v1/videos/{video_id}/content`

核心实现：

1. 新增视频代理路由，创建接口解析 `multipart/form-data`，支持 `input_reference[]` 与兼容字段 `input_reference`。
2. 在 `ProxyService` 增加视频请求处理：
   - 校验 `model`、`prompt`、`seconds`、`size`、`resolution_name`、`preset`
   - 校验模型类型为 `video`
   - 支持 `billing_type=image_credit/free`
   - 按 `image_credit_multiplier` 作为单次视频生成积分成本
   - 通过模型渠道映射选择上游 grok2api 渠道
   - 使用 multipart 转发到上游 `/v1/videos`
   - 上游成功创建任务后扣减积分并记录请求日志
3. 保存 `video_id -> channel/model/user` 的短期内存映射，查询和下载优先使用创建时渠道，避免多渠道环境查询错上游。
4. 查询和下载接口不重复计费，仅鉴权后代理到上游。
5. 数据库初始化和升级脚本支持 `unified_model.model_type='video'`。
6. 管理端模型类型增加“视频”，计费文案从“图片积分”调整为“媒体积分/图片积分兼容”。
7. 用户快速开始文档补充视频 API 示例。

## 涉及文件清单

- `backend/app/api/proxy/video_proxy.py`
- `backend/app/main.py`
- `backend/app/services/proxy_service.py`
- `backend/app/services/model_service.py`
- `frontend/src/views/admin/ModelManage.vue`
- `frontend/src/views/user/QuickStart.vue`
- `backend/sql/init.sql`
- `backend/sql/initData.sql`
- `sql/initData.sql`
- `backend/sql/upgrade_grok_video_model_20260528.sql`
- `docs/grok-video-integration.md`
- `md/impl-视频-Grok接入-20260528.md`

## 实施步骤概要

1. 增加视频代理路由和 multipart 解析。
2. 增加 `ProxyService` 视频创建、查询、内容代理逻辑。
3. 增加模型服务对视频模型的序列化能力字段。
4. 更新 FastAPI 路由注册。
5. 更新 SQL 初始化和升级脚本，允许 `model_type=video` 并预置 `grok-imagine-video`。
6. 更新管理端模型类型与计费文案。
7. 更新用户快速开始和功能文档。
8. 增加/运行语法检查与聚焦测试。
9. 创建实施记录并进行自审。
