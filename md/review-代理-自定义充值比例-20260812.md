**有效问题**

未发现阻塞项或有效缺陷。**Review 通过。**

**复核结论**

- [PaymentRechargeCallbackIntegrationTest](/Volumes/project/modelInvocationSystem/backend/test_payment_recharge_agent_rate.py:564) 使用真实 SQLite 会话、ORM 表和事务。
- 订单通过真实 `create_recharge_order()` 创建，回调直接执行真实 [`_apply_paid_order()`](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:1285)；资产入账、现金分润、结算幂等及套餐激活均未被 mock。
- 配置切换后，余额和图片充值继续使用订单中的 `1:1` 到账及 `85.71` 元代理分润快照。
- 重复余额回调后，资产流水、现金流水和结算记录均保持一条。
- 存量套餐订单在开启自定义定价后仍能完成激活并生成代理销售记录。
- 套餐列表返回严格的 `{"list": [], "total": 0}`，且不会查询套餐模板。
- [升级 SQL](/Volumes/project/modelInvocationSystem/backend/sql/upgrade_agent_custom_recharge_rate_20260812.sql:1) 在 Git 索引中是完整的 80 行新增文件；索引与工作树 blob 哈希均为 `3e3ae7f7b0fdf0d5df2c914d7db3c646d34dbb91`。三个字段幂等守卫及越界数据 `SIGNAL SQLSTATE '45000'` 均存在。

验证结果：专项测试 `27/27`、当前可发现后端测试 `55/55`、`compileall`、六个相关页面 ESLint 和前端构建均通过。已排除 `.gitignore`、18 个历史测试删除及其他并行业务变更。
