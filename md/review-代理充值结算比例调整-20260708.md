**审查结论**

未发现阻塞性问题，实现符合需求。余额充值和图片积分充值的代理结算倍率已调整为 `1:7`，用户到账倍率仍为 `1:5`，套餐返现逻辑保持 `sale_price_cny - agent_cost_price_cny` 不变。

**发现与建议**

- Low：缺少自动化回归测试。当前只有函数级手工验证，建议补充 pytest 覆盖余额充值、图片积分充值、直营网充值、套餐不受影响这几类结算场景。
- Low：生产环境变量仍需上线确认。`backend/.env` 未被 Git 跟踪，生产如果还有环境变量覆盖为 `10`，运行结果仍会是旧比例。
- Info：部分历史 `md/` 文档仍有 `1:10` 示例，运行无影响，但若被运维/客服引用，建议标注为历史规则。

**核对依据**

- 默认配置已改为 `7`：[backend/app/config.py](/Volumes/project/modelInvocationSystem/backend/app/config.py:124)
- 余额/图片积分计算仍按配置读取并快照订单：[payment_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:383)
- 套餐返现仍按售价减拿货价：[payment_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:419)
- 代理现金余额仍按订单 `agent_income_cny` 入账，套餐跳过：[payment_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:1186)

我已把完整 Review 写入：[review-代理充值结算比例调整-20260708.md](/Volumes/project/modelInvocationSystem/md/review-代理充值结算比例调整-20260708.md:1)

验证已执行：函数级结算验证通过，`py_compile` 语法检查通过。
SD_SETTLEMENT_RATE=7
RECHARGE_IMAGE_CREDIT_AGENT_CNY_RATE=7
```

### Info：历史说明文档存在旧比例描述

代码搜索发现部分历史 `md/` 实施或计划文档仍记录旧的 `1:10` 示例。这些属于历史记录，不影响运行逻辑；如果其中有被运维或客服继续引用的文档，建议另行更新或标注“历史规则”。

## 核心代码核对

- `backend/app/config.py`：`RECHARGE_AGENT_CNY_TO_USD_SETTLEMENT_RATE` 已为 `Decimal("7")`，`RECHARGE_IMAGE_CREDIT_AGENT_CNY_RATE` 已为 `Decimal("7")`。
- `PaymentService._calculate_amounts()`：余额和图片积分继续读取配置倍率计算分润，公式未被额外改动。
- `PaymentService._load_subscription_plan_for_purchase()`：套餐返现仍为 `sale_price - agent_cost`。
- `PaymentService.create_recharge_order()`：新订单继续快照 `agent_settlement_rate` 和 `agent_income_cny`。
- `PaymentService._credit_agent_cash_balance()`：支付成功后仍按订单上的 `agent_income_cny` 增加代理现金余额；套餐订单直接跳过现金余额入账。

## 验证结果

已复现函数级验证：

```text
balance_agent_rate= 7
image_agent_rate= 7
platform_balance 500.000000 0.000 7 0.00
agent_balance 500.000000 0.000 7 28.57
agent_image_credit 0.000000 500.000 7 28.57
subscription 0.000000 0.000 0.000000 0.00
```

已执行语法检查：

```bash
python -m py_compile app/config.py app/services/payment_service.py app/services/agent_cash_service.py app/services/agent_subscription_sale_service.py
```

结果：通过。

## 最终判断

实现符合本次需求，可以通过审查。建议在上线前补充自动化回归测试，并确认生产运行环境变量实际为 `7`。

## 迭代处理记录

- 已补充 `backend/test_payment_recharge_agent_rate.py`，覆盖余额充值、图片积分充值、直营网充值和套餐类型不受充值倍率影响。
- 已执行 `python -m unittest test_payment_recharge_agent_rate.py`，4 个测试通过。
- 生产环境变量确认仍作为上线检查项保留。
