**审查结论：需修改后通过**

**发现问题**

1. **高：数据库迁移记录不完整，现有库可能直接运行失败。**  
   实现新增并使用了 `subscription_plan.sale_price_cny / agent_cost_price_cny / online_sale_enabled` 和 `payment_recharge_order.subscription_*` 字段，但 impl 的“数据库更新”只声明执行了 `agent.subscription_online_recharge_enabled`。如果线上库只按 impl 执行，会在套餐列表、下单或订单序列化时触发未知列错误。  
   相关位置：[log.py](/Volumes/project/modelInvocationSystem/backend/app/models/log.py:445)、[payment.py](/Volumes/project/modelInvocationSystem/backend/app/models/payment.py:29)、[subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:651)、[payment_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:489)。  
   建议：把 [upgrade_user_subscription_purchase_20260622.sql](/Volumes/project/modelInvocationSystem/backend/sql/upgrade_user_subscription_purchase_20260622.sql:18) 纳入 plan/impl 和部署步骤，并明确必须执行。

2. **中：代理套餐开关迁移不可重复执行。**  
   [upgrade_agent_subscription_online_recharge_20260622.sql](/Volumes/project/modelInvocationSystem/backend/sql/upgrade_agent_subscription_online_recharge_20260622.sql:1) 直接 `ALTER TABLE ADD COLUMN`，如果本地/生产已手动加过列或部署脚本重跑，会因重复列失败。项目里另一个同日迁移已经用 `information_schema` 做了幂等处理。  
   建议：改成 `IF NOT EXISTS` 风格的幂等迁移。

3. **中：套餐更新服务对“局部更新”不安全。**  
   [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:579) 在 `_validate_plan_payload(..., is_update=True)` 里会把未传的 `sale_price_cny / agent_cost_price_cny / online_sale_enabled` 默认归零。当前前端编辑弹窗会提交完整表单，所以暂时不触发；但如果后续为了“排序”做单字段更新，会意外清空售价并下架套餐。  
   建议：更新场景只 normalize 请求中实际存在的字段，缺省字段保持原值。

**测试情况**

已复跑 impl 记录中的后端测试：

`python -m unittest test_subscription_plan_quota_edit.py test_user_subscription_purchase.py test_admin_payment_order_time.py`

结果：`27 tests OK`。

**建议补充测试**

补上服务端单测覆盖：代理套餐在线充值关闭时 `/subscription-plans` 返回空、创建 `subscription` 订单返回 `AGENT_SUBSCRIPTION_ONLINE_RECHARGE_DISABLED`、可购套餐按 `sort_order ASC, id ASC` 排序。当前这些主要是手工验证记录，自动回归不足。

---
## Review v2 - 修复后复审
结论：通过。上一轮 review 的三个问题均已解决。

核对结果：

- 前置 SQL 顺序已明确：plan 在 [plan-代理套餐在线充值与套餐排序-20260622.md](/Volumes/project/modelInvocationSystem/md/plan-代理套餐在线充值与套餐排序-20260622.md:43) 写明必须先执行 `upgrade_user_subscription_purchase_20260622.sql`，再执行 `upgrade_agent_subscription_online_recharge_20260622.sql`；impl 在 [impl-代理套餐在线充值与套餐排序-20260622.md](/Volumes/project/modelInvocationSystem/md/impl-代理套餐在线充值与套餐排序-20260622.md:35) 也记录了前置迁移。
- `upgrade_agent_subscription_online_recharge_20260622.sql` 已幂等：通过 `DROP PROCEDURE IF EXISTS`、检查 `agent` 表存在、检查目标字段不存在后才 `ALTER TABLE`，重复执行不会重复加列，见 [upgrade_agent_subscription_online_recharge_20260622.sql](/Volumes/project/modelInvocationSystem/backend/sql/upgrade_agent_subscription_online_recharge_20260622.sql:3)。
- 套餐模板局部更新不会默认清空相关字段：`is_update=True` 时 `sale_price_cny`、`agent_cost_price_cny`、`online_sale_enabled` 只在入参存在时归一化；`reset_period`、`reset_timezone` 也只在创建或入参显式传空时补默认值，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:579)。最终赋值也有 `field in payload` 保护，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:752)。

剩余问题：无。
