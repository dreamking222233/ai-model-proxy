# 回退缓存优化保留重试容错 Review

## 结论

实现符合当前目标：缓存策略和路由优化已从最新代码中反向撤销，`088070d` 的重试与错误脱敏逻辑保留。

## 检查点

- `_resolve_mapped_upstream_target` 回到显式 `responses:` 才选择 Responses 的行为。
- `ChannelAffinityService` 及其配置、SQL、测试已移除。
- Prompt Cache 控制策略和 normalize scope 配置已移除，保留基础 Prompt Cache 服务能力。
- `_should_retry_upstream_status`、`_resolve_runtime_retry_attempts`、`_build_user_visible_upstream_request_error`、`_sanitize_upstream_service_exception_for_user` 仍存在。
- 新增 `auth_unavailable providers=codex model=gpt-5.4` 测试，确认用户侧不暴露上游原文，日志仍保留原文。

## 验证结果

```bash
env PYTHONPATH=backend python -m py_compile backend/app/services/proxy_service.py backend/app/services/anthropic_prompt_cache_service.py backend/test_proxy_retry_error_sanitization.py
env PYTHONPATH=backend python -m unittest backend/test_proxy_retry_error_sanitization.py
```

结果：通过，28 个测试。

## 风险

服务器当前手动回滚到 `e4c3775` 会继续缺少重试/错误脱敏修复，不适合长期运行。应部署本次新提交。
