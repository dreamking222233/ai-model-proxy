**审查结论**

未发现新的功能性问题；本轮重点 3 项我认为已关闭。

- 历史 `cost_usd=50/60` 分界已改为 `2026-06-18 14:00`：见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:40) 和兼容判断 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:262)。
- 自定义“畅享”模板/快照不会再被名称误伤：兼容识别只看内置 `plan_code/plan_code_snapshot`，不再按 `plan_name` 包含“畅享”判断，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:195) 和 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:243)。
- 管理员显式配置 `cost_usd=50/60` 的新快照会按保存值解析：新快照不命中 14:00 前历史分支，测试覆盖见 [test_subscription_plan_quota_edit.py](/Volumes/project/modelInvocationSystem/backend/test_subscription_plan_quota_edit.py:58) 和 [test_subscription_plan_quota_edit.py](/Volumes/project/modelInvocationSystem/backend/test_subscription_plan_quota_edit.py:76)。

**优化建议**

建议再补两个低成本边界测试：`created_at == 2026-06-18 14:00:00` 应按新快照处理，以及历史 `cost_usd=60` 在 14:00 前仍走 `use_official_cost=True`。当前已有历史 `50` 和新快照 `60` 覆盖，但这两个边界能把分界规则锁得更严。

我已运行 focused 后端测试：

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=backend python -m unittest backend/test_subscription_plan_quota_edit.py
```

结果：13 个用例全部通过。前端 `npm run build` 我没有在当前只读沙箱里复跑，因为构建会写入产物。

## 迭代处理

已按优化建议补充两个边界测试：

- `created_at == 2026-06-18 14:00:00` 的 `cost_usd=50` 快照按新显式配置处理，`use_official_cost=False`。
- `2026-06-18 14:00:00` 前的历史 `cost_usd=60` 内置畅享快照继续 `use_official_cost=True`。

补充后执行：

```bash
env PYTHONPATH=backend python -m unittest backend/test_subscription_plan_quota_edit.py
```

结果：15 个用例全部通过。
