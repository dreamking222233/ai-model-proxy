# 实施后 Review：套餐赠送额度（2026-09-03）

## 结论

当前提交完成了数据结构、管理端入口和基础赠送扣费接入，基础验证通过；距离 Plan 要求的 enforce 级计费安全仍有关键缺口，建议保持功能灰度，不开启全量活动。

## 已验证

- 后端编译通过，套餐历史回归 32 项通过，Responses/缓存计费回归 7 项通过。
- 前端 lint 与 production build 通过。
- 套餐模板可保存 `all_models/selected_series` 及模型系列集合。
- 用户套餐保存系列快照字段，并创建实例系列关联记录。
- 模型支持 `bonus_quota_enabled`，非法的 image_credit/free 组合会被校验。
- 赠送批次支持固定天数、跟随套餐到期、幂等键、列表和取消。
- 赠送扣费按到期时间和批次 ID 顺序消费 rolling 24h 周期，并记录消耗量。

## 关键缺口

1. reservation/allocation 状态机尚未落地，赠送周期当前直接增加 used，仍需请求级预占与结算释放。
2. 普通 Token 余额完整覆盖、套餐越限分支和并发资金预占仍需按 Plan A1/A2 完成并加入回归测试。
3. 三来源分摊明细尚未写入 allocation，日志展示仍以原有 billing_mode 为主。
4. 三份初始化 SQL尚未同步新增字段和表，当前仅提供升级 SQL。
5. 流式、WebSocket、缓存、异步视频尚未全部接入统一生命周期。
6. Channel 计费证据模式已加入 ORM/API，但迁移后的配置断言和视频 finalizer 尚未实现。

## 后续动作

按 `md/tasks-套餐-赠送额度-20260903.md` 继续完成 A/B/C 阶段剩余任务；在真实 MySQL 并发、故障注入、迁移重跑及不变量测试通过前，保持 shadow 或小范围 canary。

## 本轮展示功能复核（2026-09-03）

- 用户端赠送额度卡片已确认使用 methods 中的格式化函数，能够按批次显示真实每日额度、已用、剩余、到期及模型系列。
- 管理端当前套餐用户表已显示赠送额度与模型系列；赠送弹框支持模型系列多选并在创建后刷新列表。
- 套餐模板编辑会回填 `model_scope/model_series`，避免编辑时丢失模型范围。
- 修复 `_build_quota_strategy` 缺少 `return` 的回归问题；套餐相关 32 项测试、前端 lint/build 均通过。
- 服务器已部署提交 `1f3b429`，Nginx reload、后端 8085 重启及 `/health` 检查通过。

## 套餐管理 500 回归修复（2026-09-03）

- 线上错误根因为 `_empty_usage_summary()` 在套餐列表汇总路径中构造字典后未返回，导致 `_merge_usage_rows` 对 `None` 下标赋值。
- 已恢复返回值并补充套餐回归测试验证；提交 `b86a154` 已部署，套餐列表与 active-users 接口不再触发该异常。
