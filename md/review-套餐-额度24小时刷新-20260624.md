**审查结论**

未发现阻断上线的需求偏差。核心逻辑已按 `user_subscription.start_time` 计算 rolling 24h 周期，管理端、代理端、用户端购买链路通过 `SubscriptionService` 复用后可以继承新规则；旧 0 点周期也会在命中后按新窗口重建快照。后端单测、`py_compile`、前端 lint 我已复现通过。

**发现与建议**

1. [backend/app/services/subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:1490)  
   `_get_cycle_for_summary()` 在没有周期行时会实时调用 `_rebuild_cycle_usage_snapshot()` 聚合消费记录。用户余额页单用户影响不大，但管理端/代理端列表会对每个活跃套餐额外做聚合查询，可能形成 N+1 性能压力。建议后续优化为批量聚合，或仅用户余额详情页展示精确 `current_cycle`。

2. [frontend/src/views/user/BalanceLog.vue](/Volumes/project/modelInvocationSystem/frontend/src/views/user/BalanceLog.vue:640)  
   `subscriptionModeText` 对 unlimited 套餐只要存在 `current_cycle` 就显示“每24小时刷新”，会弱化原来的“无限套餐”语义。建议改成类似“无限套餐 / 每24小时刷新”或只对 `daily_quota` 显示“每24小时刷新”。

3. 测试覆盖建议：现有新增测试主要是 helper/stub 级，已覆盖窗口算法和快照触发。建议再补一个真实 ORM session 级用例，覆盖“旧 0 点周期 + 跨 0 点消费记录 + `_get_or_create_cycle()` 重建”的端到端场景，防止 SQL 聚合表达式或字段映射回归。

**验证**

已执行并通过：

```bash
PYTHONPATH=backend python -m unittest backend.test_subscription_plan_quota_edit backend.test_user_subscription_purchase
python -m py_compile backend/app/services/subscription_service.py backend/app/services/proxy_service.py backend/test_subscription_plan_quota_edit.py
cd frontend && npm run lint -- --no-fix
```

实现整体符合本次需求，以上问题属于优化项。
