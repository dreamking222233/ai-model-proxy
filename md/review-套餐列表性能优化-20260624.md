# 套餐列表性能优化 Review

## 审查结论

通过。

本次优化针对生产日志中 `active-users` 30-42 秒、`user/profile` 偶发 5-6 秒的问题，移除了展示读路径对 `ConsumptionRecord` 的实时大表聚合。套餐扣费写路径未改，仍由 `_get_or_create_cycle(...)` 精确创建或重建 rolling 24 小时周期。

## 核心检查

1. 额度逻辑
   - 请求前额度检查、请求后扣费仍走 `_get_or_create_cycle(...)`。
   - 日卡 0 点不刷新和满 24 小时刷新逻辑未改变。

2. 性能路径
   - `get_current_subscription_summary(...)` 默认不会扫描 `ConsumptionRecord`。
   - `list_active_subscription_users(...)` 默认不会逐条聚合消费记录。
   - 单条 `get_subscription_usage_detail(...)` 保留精确聚合能力。

3. SQL
   - 新增索引脚本幂等，可重复执行。
   - 初始化 SQL 已同步新增索引。

## 验证

已执行：

```bash
PYTHONPATH=backend python -m unittest backend.test_subscription_plan_quota_edit backend.test_user_subscription_purchase
python -m py_compile backend/app/services/subscription_service.py backend/test_subscription_plan_quota_edit.py
```

结果：通过。

