**发现**
未发现影响本需求达成的功能性问题。实现整体符合 plan/impl 描述，可以通过。

低风险测试缺口：当前测试覆盖了后端 service 的排序、快到期筛选、无效套餐排除、用户级去重，但没有直接覆盖 `active-users` 的关键字搜索、API 路由参数层、以及前端“发放/取消后刷新”的交互行为。相关测试集中在 [test_subscription_compatibility.py](/Volumes/project/modelInvocationSystem/backend/app/test/test_subscription_compatibility.py:1847)。

**核对结果**
- 当前有效套餐用户：后端只查询 `active/null/""` 且 `start_time <= now <= end_time`，符合要求，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:1601)。
- 用户级去重：用 `row_number() over(partition_by=user_id order by end_time desc, id desc)`，同一用户只返回结束时间最晚的一条当前有效套餐，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:1594)。
- 剩余时长排序：`sort_order=asc/desc` 映射到 `end_time asc/desc`，等价于剩余时长少/多排序，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:1631)。
- 快到期筛选：`expires_within_days` 过滤 `end_time <= now + N days`，前端提供 3/7/15 天选项，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:1622) 和 [SubscriptionManage.vue](/Volumes/project/modelInvocationSystem/frontend/src/views/admin/SubscriptionManage.vue:164)。
- 发放/取消后刷新：模板发放、旧版开通、取消套餐后都会触发 `fetchActiveUsers()`，见 [SubscriptionManage.vue](/Volumes/project/modelInvocationSystem/frontend/src/views/admin/SubscriptionManage.vue:814)、[SubscriptionManage.vue](/Volumes/project/modelInvocationSystem/frontend/src/views/admin/SubscriptionManage.vue:835)、[SubscriptionManage.vue](/Volumes/project/modelInvocationSystem/frontend/src/views/admin/SubscriptionManage.vue:925)。
- 页面入口/API：新增 `/api/admin/subscription/active-users` 和前端 `listActiveSubscriptionUsers`，路由顺序在 `/{subscription_id}/usage` 之前，不会被动态路由吞掉。

**验证**
已运行：
- `python -m py_compile backend/app/api/admin/subscription.py backend/app/services/subscription_service.py backend/app/test/test_subscription_compatibility.py`
- `env PYTHONPATH=backend python -m unittest backend.app.test.test_subscription_compatibility`，63 个用例通过
- `npm run build`，构建通过，仅有既有资源体积 warning

结论：本次 `admin/subscription` 新增“套餐用户”页满足核心需求；建议后续补一两个 API/前端交互测试来覆盖剩余低风险缺口。
