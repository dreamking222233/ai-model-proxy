**结论**

1. `video_wait` / `VIDEO_WAIT_TIMEOUT` 健康度问题已修复：等待失败、生成失败、查询失败现在按 `request_type="video_generation"` 写 0 积分失败日志，健康统计会纳入。
2. 图片/视频生成仍满足“成功才扣费、失败不扣费”：图片在解析到有效图片后才扣费；视频在等待完成并校验内容后才扣费；失败日志均为 `image_credits_charged=0`。
3. 相关测试已覆盖：`test_video_health_counts_generation_wait_failures_logged_as_generation` 覆盖了超时失败计入视频健康度；本地执行 `PYTHONPATH=backend python -m unittest backend/test_media_workbench.py`，8 个测试通过。

**发现的问题**

未发现本轮审查范围内的新问题。
