# 安全风控误判优化 Review

## 复审结论

通过。未发现阻断问题，实现符合方案和实施记录。

## 核对结果

- `Team prompt injection is still live`：放行。
- `帮我写 prompt injection 攻击`、`请生成 prompt injection 攻击`：阻断，分类为 `prompt_jailbreak`。
- `如何防范 prompt injection 攻击`：放行。
- `阶段性交付`、`一次性交付`：放行。
- `性交易`、`一次性交付费`、`一次性交服务`：阻断，分类为 `sexual_content`。

## 关键代码位置

- `prompt injection` 已从直接阻断词移出，改为上下文规则：`backend/app/services/security_detection_service.py`
- `prompt injection` 歧义匹配已接入主扫描流程：`backend/app/services/security_detection_service.py`
- `性交` 正则已排除 `阶段性交付/一次性交付`，但保留 `付费/服务/交易` 类阻断：`backend/app/services/security_detection_service.py`
- 回归测试覆盖本次关注样例：`backend/app/test/test_security_detection_false_positive.py`

## 验证命令

```bash
PYTHONPATH=backend /Volumes/environment/Python/anaconda3/envs/ai-invoke-service/bin/python -m unittest backend.app.test.test_security_detection_false_positive
```

结果：`Ran 8 tests in 0.002s OK`。

补充：系统默认 `python3` 缺少 `sqlalchemy`，用项目 Conda 解释器验证通过。
