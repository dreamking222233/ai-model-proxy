**Findings**

未发现阻断性问题。按你指定的 3 个重点看，本次修复结论是：通过。

1. 套餐每日额度用尽后的扣费逻辑正确  
   [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:267) 按“未覆盖额度 / 本次套餐消耗额度”计算余额差额；实际扣款在 [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:8575) 检查 `balance_before_decimal < amount` 会直接拒绝，因此不会扣成负数。  
   有剩余额度时只消费剩余额度并将余额部分记为 `mixed`；完全无额度时走余额扣全额。

2. 已取消/过期套餐不会继续作为套餐使用  
   [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:137) 的 active 过滤只允许 `active/null/""`，排除了 `cancelled/expired`；[subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:899) 同时按起止时间过滤。  
   刷新用户状态时，没有活跃套餐会把 `sys_user` 缓存重置为 `balance`，见 [subscription_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:919)。

3. `admin/users` 不再依赖 `sys_user` stale cache 展示取消套餐  
   `/api/admin/users` 走 `AuthService.list_users`，见 [user.py](/Volumes/project/modelInvocationSystem/backend/app/api/admin/user.py:34)。列表构造时先刷新套餐状态，再从当前套餐摘要决定展示值，而不是直接展示 `u.subscription_type`，见 [auth_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/auth_service.py:294)。

**测试**

已运行：

```bash
PYTHONPATH=backend python -m unittest backend/app/test/test_subscription_compatibility.py
```

结果：`Ran 55 tests ... OK`。  
仅有 Redis 连接被沙箱拒绝后降级关闭的提示，以及 `datetime.utcnow()` deprecation warning，不影响本次结论。
