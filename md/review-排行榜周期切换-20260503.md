**审查结论**

未发现阻断性问题。实现基本符合 `plan`/`impl` 中的需求：`user/ranking` 和 `admin/ranking` 共用组件已确认，后端 `limit` 默认 Top 10，前端切换周期时传入 `days + limit: 10`，标题/副标题已按周期动态展示，并补了请求序号防止旧请求覆盖新数据。

**优化建议**

1. 低风险体验问题：[UsageRanking.vue](/Volumes/project/modelInvocationSystem/frontend/src/views/user/UsageRanking.vue:21) 的 Top 3 podium 不在 `<a-spin>` 内，切换周期后新请求返回前，页面标题会先变成新周期，但 podium 仍显示旧周期数据。若希望完全避免误导，可以在发起新请求时清空 `ranking`，或把 podium 也纳入 loading 状态。

2. 测试覆盖建议：后端测试已覆盖接口默认值、参数传递、非法 `limit` 和服务层 clamp；建议后续补一个前端单测或 e2e，锁定“切换 1/7/30 天时请求参数正确、旧请求不会覆盖新请求”的行为。

**验证结果**

已执行：

```bash
cd backend && python -m unittest app.test.test_user_stats_ranking
cd frontend && npm run build
```

结果：后端 5 个用例通过；前端构建通过，仅保留既有 asset size / entrypoint size warning。
