**审查结果：未发现阻塞问题。**

实现符合方案目标：微信入账校验已从优先 `payer_total` 改为优先 `amount.total`，优惠支付场景不会再因用户实付金额较低被误判；缺失 `total` 时保留 `payer_total` 兜底；异常金额 payload 会拒绝入账，避免错误数据进入结算流程。前端充值页也按要求展示了未到账提醒，并复用当前站点的微信/QQ 配置。

验证已通过：

```bash
cd backend && python -m pytest app/test/test_payment_recharge.py
# 15 passed

cd frontend && npm run lint -- src/views/user/Recharge.vue
# No lint errors found
```

优化建议：

1. `backend/app/services/payment_service.py:1003` 附近可以考虑抽出一个 `_extract_wechat_order_amount()` 小函数，后续如果微信金额字段扩展或其他入口复用，测试会更聚焦。
2. 当前测试覆盖 `_apply_paid_order` 很充分，但还可以补一个 `sync_order_status` 查单路径的集成式单测，确认真实查单返回的 `amount.total/payer_total` 结构也能完整走通。
3. 测试输出里有既有 `datetime.utcnow()` 弃用 warning，不影响本次功能，但后续可统一切到 timezone-aware 写法。
