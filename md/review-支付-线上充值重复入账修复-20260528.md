**结论**

有条件可发布。核心幂等方案符合需求：回调先校验 payload，再通过 `payment_recharge_settlement(order_no, asset_type)` 唯一键 claim 入账资格，资产变更、分润、订单置 `paid` 在同一事务提交内完成。支付单测已通过：`17 passed`。

**需修复/确认**

1. **中风险：初始化 SQL 未同步新表**
   现有 `backend/sql/init.sql`、`backend/sql/initData.sql`、`sql/initData.sql` 未包含 `payment_recharge_settlement`。存量生产库执行升级脚本没问题，但新环境如果只跑 init 脚本会缺表，支付回调会失败。建议本次发布前同步这些基线 SQL，或明确发布流程一定先执行升级脚本。

2. **低风险：迁移脚本对“半执行旧表”场景不够强**
   [upgrade SQL](/Volumes/project/modelInvocationSystem/backend/sql/upgrade_payment_recharge_settlement_idempotency_20260528.sql:26) 会在唯一键缺失时直接 `ALTER TABLE ADD UNIQUE KEY`。如果生产曾手动建过该表且已有重复 settlement 行，脚本会失败。建议加发布前检查或在脚本中先查询重复：
   `GROUP BY order_no, asset_type HAVING COUNT(*) > 1`，有重复则中止并提示人工处理。

3. **低风险：唯一键冲突测试偏 mock**
   [test_payment_recharge.py](/Volumes/project/modelInvocationSystem/backend/app/test/test_payment_recharge.py:247) 用 patch `flush` 模拟 `IntegrityError`，能覆盖分支判断，但没有覆盖真实 DB 唯一键冲突与 `begin_nested()` 回滚行为。建议补一个真实插入冲突测试，或并发双 session 测试，增强回归信心。

**关键审查点**

- 幂等 claim 逻辑在 [payment_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:983)。
- 重复 settlement 会跳过资产入账，并补订单 `paid` 状态，符合方案要求：[payment_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:1138)。
- `IntegrityError` 只识别 settlement 唯一键冲突，其他完整性错误继续抛出，方向正确：[payment_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/payment_service.py:959)。
- 迁移脚本建表、补唯一键、回填历史已支付订单逻辑基本可执行：[upgrade SQL](/Volumes/project/modelInvocationSystem/backend/sql/upgrade_payment_recharge_settlement_idempotency_20260528.sql:4)。

发布前必须执行升级脚本，并确认表与唯一键存在后再重启所有后端实例。

**迭代处理记录**

- 已处理第 2 点：升级脚本增加 `payment_recharge_settlement` 重复数据检查，若半执行表存在重复 `order_no + asset_type`，发布时会明确查询出来，需先人工清理再继续。
- 已处理第 3 点：唯一键冲突测试改为真实 SQLite 唯一键冲突路径，覆盖 `begin_nested()` 回滚后 session 仍可继续使用；非重复完整性错误继续使用 mock 验证会抛出。
- 第 1 点不阻塞当前存量线上发布：本次生产发布必须执行 `backend/sql/upgrade_payment_recharge_settlement_idempotency_20260528.sql`，并确认表与唯一键存在。现有 `backend/sql/init.sql` / `backend/sql/initData.sql` 本身未包含完整在线支付表，基线初始化脚本建议后续单独统一同步。

迭代后验证：

```bash
cd backend && python -m pytest app/test/test_payment_recharge.py
```

结果：`17 passed`，仅有既有 deprecation warnings。
