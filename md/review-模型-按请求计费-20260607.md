结论：**通过**。

上一轮两个高优先级问题已解决：

- 按次计费预检已覆盖长上下文倍率：未知输出上限时会保守把上下文估算提升到阈值以上，并使用长上下文倍率参与 `estimated_total_cost` 计算，见 [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:1519)。
- 套餐不足且余额不足时，不再允许按次请求被套餐超额覆盖：预检阶段会计算套餐后需余额承担的金额并要求足额余额，最终记账阶段 `exact_request_billing` 分支也会直接抛错，不再走 `allow_quota_over_limit`，见 [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:203) 和 [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:9329)。

未发现新的明显阻断问题。按次计费的最终扣费公式与预检公式一致，都是 `request_price * price_multiplier * fast_multiplier * context_multiplier`，见 [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:9206)。

验证：后端相关文件 `python3 -m py_compile ...` 通过。额外尝试直接 import `ProxyService` 做小脚本演算时，当前 shell 环境缺少 `fastapi` 依赖，未执行成功；不影响源码语法编译结论。
