结论：**通过，无 P0/P1 阻断问题。** 第二轮 WebSocket 混合回退问题已修复。仍有 1 个 P2 工程问题。

1. **P2：新增透传测试仍未纳入 Git。**
   [.gitignore:111](/Volumes/project/modelInvocationSystem/.gitignore:111) 仍忽略 `/backend/test_*.py`；`git check-ignore` 显示 [test_channel_passthrough.py](/Volumes/project/modelInvocationSystem/backend/test_channel_passthrough.py:1) 为 `!!`，`git ls-files` 也确认其未被跟踪。因此第二轮新增的 `A -> B -> C` 回退测试及其他透传测试不会进入正常提交和 CI，与 review-r2 中“通过 `git add -f` 纳入提交”的记录不符。

重点复核结果：

- **ORM 快照足够**：channel/model 在候选生成时完成列快照，user/API key 在释放 Session 前完成快照。HTTP/SSE 和 WebSocket 最终计费通过新的短生命周期 Session 重新加载持久化对象，没有发现 Session 关闭后访问失效 ORM 属性的问题。
- **请求响应字节保持**：HTTP 原始 body 直接作为 `content` 发送；HTTP 响应实体和 SSE chunk 未重建；WebSocket 文本及二进制帧原样发送。解压后的响应实体会同步移除 `content-encoding`、`content-length`，符合方案定义的实体字节保真范围。
- **混合回退已修复**：[channel_passthrough_service.py:806](/Volumes/project/modelInvocationSystem/backend/app/services/channel_passthrough_service.py:806) 现在严格逐个遍历透传和普通候选；普通候选仅在首轮未向客户端发送内容且可重试时返回外层继续。已输出内容后的异常被标记为不可重试，不会再调用下一渠道。
- **计费防重成立**：SSE 统一在读取任务 `finally` 结算；WebSocket 通过轮次 `finalized` 和 response id 集合防止重复终止事件重复计费；本地计费失败不会触发重新调用上游。

只读定向验证结果：透传测试 `18 passed`；旧 WebSocket 相关回归 `3 passed`；`git diff --check` 通过。

## P2 处理

提交阶段使用 `git add -f backend/test_channel_passthrough.py` 强制纳管新增测试，并在提交前通过 `git ls-files` 复核。
