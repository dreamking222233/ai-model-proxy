审查结论：**无阻塞性问题，可以合并**。充值开关判断、实际比例判断和人民币换算公式整体正确，接口新增字段也保持了向后兼容。

发现的问题和建议：

1. **P1：新增测试和文档被 `.gitignore` 忽略**
   - [`/.gitignore:109`](/Volumes/project/modelInvocationSystem/.gitignore:109) 忽略整个 `md/`。
   - [`/.gitignore:111`](/Volumes/project/modelInvocationSystem/.gitignore:111) 忽略 `backend/test_*.py`。
   - 因此实施记录中的测试文件和方案文档不会进入 Git 变更，也不会被 CI 正常执行。建议调整忽略规则，或显式 `git add -f` 纳入交付。

2. **P2：测试没有覆盖真实策略解析链路**
   - [`test_user_models_cny_pricing.py:10`](/Volumes/project/modelInvocationSystem/backend/test_user_models_cny_pricing.py:10) 直接 mock `resolve_user_recharge_policy()`。
   - 未覆盖：
     - 平台支付开关全部关闭；
     - 代理 `online_recharge_enabled=0`；
     - 自定义充值比例为 5 时显示；
     - 自定义充值比例非 5 时隐藏；
     - `/api/user/models` 实际返回字段。
   - 建议增加基于真实 `AgentService._build_recharge_policy()` 和接口函数的集成测试。

3. **P2：前端对“仅 Token 模型展示”依赖隐式分支**
   - [`ModelList.vue:189-207`](/Volumes/project/modelInvocationSystem/frontend/src/views/user/ModelList.vue:189) 当前通过 `v-else` 进入 Token 价格区域。
   - 目前合法计费类型只有 `token/request/image_credit/free`，所以现状正确；但若未来增加未知或其他计费类型，可能错误显示人民币 Token 价格。
   - 建议显式增加 `model.billing_type === 'token'` 判断，并补充非 Token 模型展示测试。

4. **P2：方案文档与实际接口字段命名不一致**
   - 方案写的是页面级 `show_cny_price`；
   - 实现返回的是每个模型项的 `cny_price_enabled` 和 `cny_price_rate`。
   - 当前前后端自洽且对旧客户端是追加字段，不构成运行时兼容性问题，但应统一文档和接口契约，避免后续客户端误用。

已确认：

- [`models.py:19-29`](/Volumes/project/modelInvocationSystem/backend/app/api/user/models.py:19) 使用 `online_recharge_enabled` 和实际 `balance_recharge_rate`，不会误用套餐充值开关。
- `Decimal("5")` 精确判断充值比例。
- 人民币价格按 `美元价格 / 5` 计算，公式正确。
- 非 `image_credit`、`request`、`free` 分支不会展示当前提示。
- 后端相关测试：3 项目标测试、27 项充值策略测试均通过。
- `compileall`、ESLint、前端生产构建均通过；仅有 Redis 不可用回退提示和既有资源体积 warning。

## 整改记录

- 通过 `.gitignore` 精确放行本次方案、实施、审查文档和专项测试文件。
- 新增真实 `AgentService._build_recharge_policy()` 场景测试，覆盖标准比例、自定义非 5 倍和关闭在线充值。
- 将 `ModelList.vue` 价格区域改为显式 `billing_type === 'token'` 分支。
- 将方案中的字段名统一为实现使用的 `cny_price_enabled` 和 `cny_price_rate`。

整改后复核：无阻塞性问题，可以合并。
