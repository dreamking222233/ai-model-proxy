# 用户-模型展示 Review

## 审查结论

实现符合“恢复 `user/models` 价格和上下文展示，并兼容按请求计费”的需求，未发现阻塞上线的问题。

## 已发现并修复的问题

1. 小屏可能横向溢出：已在 `max-width: 480px` 下将模型网格改为单列，并降低页面横向 padding。
2. 免费模型中部价格区显示 `$0 /1M`：已增加 `billing_type === 'free'` 分支，直接显示“免费使用”。

## 验证

- `npm run lint -- --no-fix src/views/user/ModelList.vue`：通过。
- `npm run build`：通过，存在历史资源体积 warning，不影响本次修复。
- `/api/user/models` 返回的 `input_price`、`output_price`、`max_tokens`、`billing_type`、`request_price` 字段均已在页面展示逻辑中消费。
