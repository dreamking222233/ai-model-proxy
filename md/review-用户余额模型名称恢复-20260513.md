## Review 结果

### 结论

实现符合当前需求，修复范围小，未改变用户接口结构。

### 检查项

1. 是否恢复了用户端模型名称展示
   - 是
   - 当 `ConsumptionRecord.model_name` 为空时，会回填 `RequestLog.requested_model`

2. 是否泄露内部字段
   - 否
   - 未返回 `actual_model`、`channel_name`

3. 是否影响管理员日志展示
   - 否
   - 修改仅在 `BalanceService.get_consumption_records()` 内

4. 是否存在明显性能问题
   - 风险较低
   - 查询按当前分页结果的 `request_id` 批量回填，不是逐条查询

### 建议

后续如果还有类似问题，可统一收敛为：

- 用户端展示统一使用 `requested_model`
- 管理端展示 `requested_model + actual_model + channel_name`

这样角色边界会更稳定。
