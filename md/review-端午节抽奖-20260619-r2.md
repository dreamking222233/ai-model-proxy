复审结论：通过，上一轮 3 个问题均已修复，未发现阻塞项。

关键确认：

- 仅终端用户可报名：已在 service 层校验 `user.role == "user"`，状态查询和报名都会走该校验：[dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:167)、[dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:238)、[dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:248)
- 消费资格口径已改为真实模型请求：统计 `ConsumptionRecord.total_cost > 0` 且 `request_id/model_name` 非空，人工扣款、代理回收写入的流水这两个字段为空，会被排除：[dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:151)
- `IntegrityError` 不再全部伪装成功：报名提交冲突后只有能查到已有报名记录才幂等返回，否则抛 `LOTTERY_REGISTER_CONFLICT`：[dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:281)

验证：已执行 targeted `python -m py_compile`，通过。

剩余风险：目前主要是缺少针对这 3 个回归点的自动化测试；另外 `IntegrityError` 幂等判断没有解析具体唯一键名，但行为上已不再把所有完整性错误都返回成功。
