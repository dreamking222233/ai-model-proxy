**Findings**

- Medium: [dragon_boat_lottery.py](/Volumes/project/modelInvocationSystem/backend/app/api/user/dragon_boat_lottery.py:19) / [dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:229)  
  用户报名接口只依赖 `get_current_user`，服务层也没有校验 `user.role == "user"`。管理员或代理账号如果直接调用 `/api/user/dragon-boat-lottery/register`，满足余额/套餐条件时也会进入抽奖池。建议在 API 或 service 层显式限制终端用户报名。

- Medium: [dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:200)  
  “消费大于 100”直接使用 `UserBalance.total_consumed`，但现有扣减/回收路径也会增加该字段，例如 [balance_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/balance_service.py:145) 和 [agent_asset_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/agent_asset_service.py:379)。这会让被管理员扣款或代理回收余额的用户获得“消费资格”，不一定等同于真实模型消费。建议按 `ConsumptionRecord` 中真实请求消费记录统计，排除人工扣减、代理回收等 `request_id/model_name` 为空的流水。

- Low: [dragon_boat_lottery_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/dragon_boat_lottery_service.py:260)  
  报名时捕获所有 `IntegrityError` 后直接返回当前状态，只有重复报名冲突适合这样处理。其他完整性错误会被伪装成成功响应，前端还会显示“报名成功”。建议只对 `uk_dragon_boat_lottery_user` 做幂等处理；回滚后若查不到报名记录，应抛出业务异常或重新抛出原错误。

**验证**

已执行：

- `python -m py_compile ...` 通过
- `npm run build` 通过，仅有既有 bundle 体积告警

整体实现与方案主流程基本一致：表结构、报名、资格检测、管理端列表、抽奖持久化和前端路由都已落地。主要需要补上角色边界和消费统计口径这两个业务风险。
