**发现**
- 中：[backend/sql/init.sql](</Volumes/project/modelInvocationSystem/backend/sql/init.sql:383>) 和 [backend/app/models/log.py](</Volumes/project/modelInvocationSystem/backend/app/models/log.py:29>) 不一致：ORM 已定义 `idx_request_log_agent_id_id`，但 backend 初始化脚本没加这个索引。用 `backend/sql/init.sql` 新建的库，`agent_only` / 代理侧日志查询拿不到这条优化。
- 中：[backend/sql/upgrade_admin_request_logs_perf_20260513.sql](</Volumes/project/modelInvocationSystem/backend/sql/upgrade_admin_request_logs_perf_20260513.sql:1>) 也漏了同一个 `agent_id + id` 索引，存量库即使执行升级脚本，代理日志的热路径仍然缺索引。对比之下，[sql/upgrade_admin_request_logs_perf_20260513.sql](</Volumes/project/modelInvocationSystem/sql/upgrade_admin_request_logs_perf_20260513.sql:1>) 是完整的，说明这里是 backend 副本同步遗漏。

**建议**
- 把 backend 侧两份 SQL 一并补齐 `idx_request_log_agent_id_id`。
- 再补一个接口级回归测试，覆盖 `agent_only/platform_only/user_id/model/status/date` 组合筛选，避免以后两阶段查询和前端参数再出现偏差。

其余两阶段查询、前端参数传递和返回结构对齐，整体实现方向是对的。
