1. 高风险：并发请求下，已经通过前置检查的请求，后置记账时仍可能被改判为余额计费或直接失败。根因在 [subscription_service.py:1021](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:1021) 的 `usage_guard` 和 [subscription_service.py:1208](/Volumes/project/modelInvocationSystem/backend/app/services/subscription_service.py:1208) 的二次阈值判断；如果另一条并发请求先把额度打到阈值以下，后完成的这条请求即使已被放行，也会在 [proxy_service.py:8127](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:8127) 走余额兜底或抛错。这和方案里的“本次放行、下次拦截”不一致。

2. 中风险：上述后置改判如果发生在非流式请求上，会被包装成 500 返回给用户。 [proxy_service.py:8426](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:8426) 只特判了 `SUBSCRIPTION_UNLIMITED_DAILY_TOKEN_EXCEEDED`，没有同样处理 `SUBSCRIPTION_DAILY_QUOTA_EXCEEDED`，所以一个实际上已经成功完成的上游请求，可能在本地记账阶段被覆盖成 `TEXT_BILLING_FAILED`，诱发客户端重复重试。

其余部分我看下来，前置 `0.1` 阈值规则和 OpenAI / Anthropic / Responses 入口接入是符合方案的。审查结论已经写入 [review-计费-请求前额度余额预检查-20260521.md](/Volumes/project/modelInvocationSystem/md/review-计费-请求前额度余额预检查-20260521.md:1)。

我还跑了 `unittest` 和 `py_compile`，当前测试是通过的；但现有测试没有覆盖这个并发后置记账场景，建议下一步优先补这组竞态回归测试。
ILED` 500。这样用户会收到失败结果，但上游请求其实已经执行成功，容易诱发重复重试和重复上游消耗。

## 建议

1. 后置套餐记账不要再用“当前剩余额度是否仍高于阈值”作为更新条件。前置一旦放行，该请求就应该允许把套餐周期额度记到 `0` 或负数；并发下也应保持这一语义。
2. 如果需要防重复记账，应基于 `request_id` 做幂等保护，而不是在后置阶段再次按剩余额度回绝。
3. 补一组并发/竞态回归测试，至少覆盖：
   - 两个请求都在额度 `0.11` 以上时通过前置检查，随后依次完成；
   - 第一个请求把额度扣穿，第二个请求仍应记入套餐而不是回退余额；
   - 非流式成功响应下，本地后置记账异常不能把已成功的上游响应覆盖成 500。

## 验证记录

- `env PYTHONPATH=backend python -m unittest app.test.test_subscription_compatibility app.test.test_request_audit_hardening`
- `python -m py_compile backend/app/services/proxy_service.py backend/app/services/subscription_service.py backend/app/test/test_subscription_compatibility.py backend/app/test/test_request_audit_hardening.py`

当前相关测试通过，但未覆盖上述并发后置记账场景。
