**审查结论：通过**

未发现阻断问题。重点复查的 request 计费预检查已经与模型开关一致：

- [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:2060) 预检查计算倍率时传入 `unified_model`，会读取 `long_context_billing_enabled`。
- [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:2092) request 计费在输出 token 未知时，只有模型开关开启才使用保守 `2x` 预检查；关闭时保持 `1x`。
- [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:10459) 真实扣费同样调用同一个倍率 helper 并传入 `unified_model`，预检查和实际扣费口径一致。
- [model_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/model_service.py:169) 新建模型未显式传值时按系列默认：GPT 开启，其它关闭；更新时未传字段不会覆盖已有值。

已执行验证：

```bash
env PYTHONPATH=backend python -m unittest backend/test_long_context_billing.py
python -m py_compile backend/app/models/model.py backend/app/schemas/model.py backend/app/services/model_service.py backend/app/services/proxy_service.py backend/test_long_context_billing.py
```

结果：4 个单测通过，Python 编译通过。单测覆盖了 request 计费关闭开关时预检查不按 2 倍估算、开启开关时保留 2 倍保守估算。
