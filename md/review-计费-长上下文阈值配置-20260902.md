## 审查结论

**通过（无阻断问题）。**

### 发现项

1. **[P2] 接口校验可接受 `null` 和布尔值**
   - [model.py:31](backend/app/schemas/model.py#L31)、[model.py:55](backend/app/schemas/model.py#L55)
   - `null` 会静默重置为 `262144`；`true` 会被转换成阈值 `1`，存在误触发双倍计费风险。
   - 建议使用前置 validator 拒绝显式 `null`、`bool`，并限制上限 `2147483647`。

2. **[P2] 缺少真实扣费、套餐耗尽和混合扣费集成测试**
   - [test_long_context_billing.py:18](backend/test_long_context_billing.py#L18)
   - 当前覆盖 helper 和预检，但未直接验证真实扣费、记账快照、纯余额及 mixed 分支。
   - 建议补充 `threshold/threshold+1`、额度刚好耗尽、部分额度加余额等用例。

3. **[P3] `admin/models` CRUD 测试覆盖不足**
   - [test_long_context_billing.py:49](backend/test_long_context_billing.py#L49)
   - 目前只验证 Schema，未验证创建默认值、更新省略字段、列表和详情返回字段。

### 已确认

- ORM、Schema、Service CRUD、管理端表单及列表字段链路完整。
- 预检、按次保守预检和真实扣费使用同一模型阈值 helper。
- 保持严格 `context_tokens > threshold`；默认 `262144` 兼容正确。
- 套餐额度、混合扣费、余额扣减控制流未被改写。
- 升级脚本幂等，三份初始化 SQL 字段定义一致。

### 验证

- 定向测试：`40 tests OK`
- 排除既有 WebSocket 失配模块：`234 tests OK`
- 全量测试：267 项，3 项既有 WebSocket 参数失配错误，与本次差异无关
- `py_compile`、`git diff --check`、前端构建均通过

完整审查记录：

[md/review-计费-长上下文阈值配置-20260902.md](/Volumes/project/modelInvocationSystem/md/review-计费-长上下文阈值配置-20260902.md)
有调用 `_deduct_balance_and_log_once`，因此未自动验证真实 usage 在 `threshold` / `threshold + 1` 下的 `total_cost`、`quota_cost`、两张记账表快照，以及额度不足时的 mixed/balance 分支。当前代码人工核对正确，但资产扣减回归保护偏弱。
- 修复建议：增加 SQLite 或可控 Session 的记账集成测试，至少覆盖纯余额、额度刚好耗尽、部分额度+余额、额度耗尽后余额回退，并断言 `context_token_threshold_snapshot=200000` 及边界倍率。

### [P3] `admin/models` CRUD 回归仅验证 Schema，未验证实际 Service 读写链路

- 位置：`backend/test_long_context_billing.py:49-54`；生产链路位于 `backend/app/services/model_service.py:613-645`、`backend/app/services/model_service.py:719-784`、`backend/app/services/model_service.py:787-868`、`backend/app/services/model_service.py:900-962`。
- 现状：用例只构造 `UnifiedModelCreate/Update`，未验证创建默认 `262144`、更新未传字段时保留旧值、列表/详情返回字段。
- 修复建议：增加 `ModelService.create_model/update_model/list_models/get_model_with_mappings` 的服务层用例，同时覆盖自定义值、缺省值和更新省略字段。

## 重点核对结果

- `admin/models` 字段链路：通过；ORM、请求 Schema、Service 序列化/CRUD、桌面及移动端列表、表单已接通。
- 预检/按次保守预检/真实扣费：通过；均使用 `_get_long_context_token_threshold`，按次无输出上限时使用模型阈值 `+ 1`。
- 边界与兼容：通过；`context_tokens > threshold` 才为 2 倍，`262144` 为 1 倍，`262145` 为 2 倍；缺字段/无效运行时值回退 `262144`。
- 套餐、混合扣费与余额：通过；新阈值只改变成本计算的上游倍率，后续分支未改动。
- 数据库：通过；幂等升级脚本仅增加 `INT NOT NULL DEFAULT 262144`，三份初始化 SQL 一致，现有 INSERT 均显式列名，不会因新列错位。

## 验证记录

- `git diff --check`：通过。
- 定向后端回归：`40 tests OK`。
- 排除已知 WebSocket 失配模块后：`234 tests OK`。
- 全量后端：`267` 项中 `3` 项既有 WebSocket 用例因缺少 3 个调用参数报错，与本次 diff 无关。
- Python `py_compile`：通过。
- 前端 `npm run build`：通过；仅有现有 Browserslist 过期和 bundle 体积警告。

## 发布注意

启动新后端前先执行 `backend/sql/upgrade_model_long_context_token_threshold_20260902.sql`，否则 ORM 查询 `unified_model` 时会因缺列失败。本次已审查 SQL 结构，未连接实际 MySQL 实例执行迁移。

## Review 后迭代结果

- 已修复接口显式 `null/true/false` 的 P2：Schema 在整数转换前拒绝这些原始值，并增加 `2147483647` 上限；Service 更新入口也拒绝显式空值，避免自定义阈值被重置。
- 已补充模型 Service 创建默认值、更新省略字段保持自定义阈值的测试。
- 已补充阈值调整后的成本在套餐额度完全耗尽、部分额度覆盖时转入余额扣费金额的断言。
- 最终定向计费回归：`42 tests OK`；排除既有 WebSocket 失配模块后的后端回归：`236 tests OK`。
- Review 后已在本地 MySQL 执行升级脚本并验证字段结构，异常阈值记录为 `0`。
