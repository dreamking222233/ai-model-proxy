**结论：通过**

阻断问题：未发现。

已确认上次关注的两个测试点已补充：

- 入口级限流测试已补：[test_billing_admission_decision.py](/Volumes/project/modelInvocationSystem/backend/app/test/test_billing_admission_decision.py:121) 验证低资产用户已有 3 个租约时，第 4 个请求抛 `BILLING_CONCURRENCY_LIMITED`，且 producer 不执行。
- 流式释放测试已补：[test_billing_admission_decision.py](/Volumes/project/modelInvocationSystem/backend/app/test/test_billing_admission_decision.py:152) 验证 `StreamingResponse` 迭代结束后释放 Redis 租约。
- 实现路径匹配测试目标：[proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:510) 在上游 producer 前 acquire；非流式、异常路径释放；流式通过 [proxy_service.py](/Volumes/project/modelInvocationSystem/backend/app/services/proxy_service.py:490) 包装 `body_iterator` 并在 `finally` 释放。

验证已运行：

```bash
PYTHONPATH=backend /Volumes/environment/Python/anaconda3/envs/ai-invoke-service/bin/python -m unittest backend.app.test.test_billing_concurrency_service backend.app.test.test_billing_admission_decision
```

结果：`Ran 12 tests ... OK`

```bash
/Volumes/environment/Python/anaconda3/envs/ai-invoke-service/bin/python -m py_compile backend/app/services/billing_concurrency_service.py backend/app/services/proxy_service.py backend/app/test/test_billing_concurrency_service.py backend/app/test/test_billing_admission_decision.py
```

结果：通过。

剩余非阻断风险：当前新增测试主要覆盖 helper/服务封装层，不是完整 HTTP 路由集成测试；但所有 `_assert_text_request_allowed` 调用点已追到并发包装或 WebSocket 手动租约路径，未发现绕过上游调用的入口。
