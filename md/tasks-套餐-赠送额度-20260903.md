# 套餐赠送额度实施 Tasks

## 阶段 A：计费安全基线
- [ ] A1 增加余额/套餐额度边界回归用例，复现正余额透支与套餐越限。
- [ ] A2 移除生产路径的套餐越限放行，余额扣减统一校验可用余额。
- [ ] A3 统一有效套餐半开区间判断。

## 阶段 B：数据模型与迁移
- [x] B1 扩展 `UnifiedModel`：赠送开关、计费配置版本。
- [x] B2 扩展套餐模板/用户套餐：模型作用域、系列版本快照、配置版本。
- [x] B3 新增模板系列关联、用户套餐系列快照（ORM）。
- [x] B4 新增赠送批次与 rolling 24h 周期表。
- [ ] B5 新增 reservation、allocation、attempt、accounting event 表。
- [x] B6 增加 Channel 视频计费证据模式。
- [x] B7 新增升级 SQL（初始化 SQL 同步待补）。

## 阶段 C：服务与 API
- [x] C1 扩展套餐模板 Schema/API，支持全部模型或指定系列多选。
- [x] C2 实现赠送批次创建、列表、详情、取消及幂等键。
- [ ] C3 实现统一报价与 Decimal 分摊函数。
- [ ] C4 实现 reservation 预占、结算、释放及状态机。
- [ ] C5 接入模型最终解析系列和赠送模型开关。
- [ ] C6 接入文本、Responses、WebSocket、缓存、视频入口。
- [ ] C7 增加对账 worker、人工处置 API、删除/套餐覆盖门禁。

## 阶段 D：管理端
- [x] D1 `/admin/subscription` 模板系列控件与共享额度提示。
- [x] D2 `/admin/subscription` 套餐用户赠送弹框、时长与额度设置。
- [x] D3 `/admin/models` 赠送模型开关、系列标签与筛选。
- [ ] D4 日志、余额和套餐明细展示三来源分摊。

## 阶段 E：验证与发布
- [x] E1 基础单元回归与编译、前端 lint。
- [ ] E2 真实 MySQL 并发、死锁、迁移重跑测试。
- [ ] E3 流式取消、断连、unknown 上游、视频终态故障注入。
- [ ] E4 off → shadow → enforce → bonus canary 发布。
- [ ] E5 创建 Impl 文档并执行最终 Review。
